[build]
builder = "nixpacks"
buildCommand = "cd server && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "cd server && npx prisma migrate deploy && node server/dist/index.js"
restartPolicyType = "on_failure"
