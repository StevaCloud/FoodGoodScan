[build]
builder = "nixpacks"
buildCommand = "npm install && ./node_modules/.bin/prisma generate"

[deploy]
startCommand = "./node_modules/.bin/prisma migrate deploy && npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
